#Mandatory Parameters
#--------------------------------------------
#Docker host/client machine address
DOCKER_HOST=abc.def.com

#Database Configurable
#Enter the database connection details in the supported format. Database Connect String. This can be specified in the following format - 
#For Oracle Database(PDB): host:port:SID  OR host:port/service
#For Oracle Database(CDB): host:port/service_name (If Oracle Container Database is used, then pass PDB service name as given in the DB_CONNECTSTRING below.)
#DB Hostname can be a container name and should be on the same network or host name if it's outside the network.
DB_CONNECTSTRING=ghi.jkl.com:1521//<DB_PDB>.<DB_DOMAIN>

DB_USER=
DB_PASSWORD=

RCU_PREFIX=DOCKER

#Optional Parameters
#----------------------------------------------------------------------------
#If not set, these properties will hold default values.
#If not set, a random password(default) will be generated, which will be printed on the console at the end of Admin Server startup.
DB_SCHEMA_PASSWORD=

#For Sites with examples set the below property to true. Defaults to 'false'. 
SAMPLES=false

#Sites domain name. Defaults to 'base_domain'.
DOMAIN_NAME=base_domain

#Sites server name. Defaults to 'wcsites_server1'.
SITES_SERVER_NAME=wcsites_server1

#Weblogic Admin Username. Defaults to 'weblogic'.
ADMIN_USERNAME=

#If not set, a random password(default) will be generated, which will be printed on the console at the end of Admin Server startup.
ADMIN_PASSWORD=